# Postgresql-fastAPI-vue-stack-demo

- Python interpeter in pycharm should be added via poetry

todo 

pytest 
ruff
black

commands dump for db mig

poetry run alembic revision --autogenerate -m "name"
poetry run alembic upgrade head
